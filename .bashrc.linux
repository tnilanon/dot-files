# .bashrc.linux

alias lso='/bin/ls'
alias ls='lso -F --color=auto'
alias ll='ls -l --time-style=long-iso'
alias lh='ll -h'

# Keychain
if command -v keychain >/dev/null 2&>1; then
	eval `keychain --eval --quiet id_ed25519 id_rsa id_github_rsa`
fi


