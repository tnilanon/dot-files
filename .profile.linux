# .profile.linux
# Platform-specific environment

if [ ! -z "${BASH_SOURCE_DEBUG+x}" ]; then
  echo 'sourcing .profile.linux'
fi

if [ ! -z "${BASH_SOURCE_DEBUG+x}" ]; then
  echo 'done sourcing .profile.linux'
fi


